import { CapacitorConfig } from '@capacitor/cli'

const config: CapacitorConfig = {
  appId: 'be.michiel.wouters.thomasmore.graduaten.gallery',
  appName: 'Gallery',
  webDir: 'www',
  server: {
    androidScheme: 'https',
  },
}

export default config